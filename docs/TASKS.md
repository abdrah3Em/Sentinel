# Hardening checklist — ICSC 2026 Track E

Baseline (harden/energy-first, Phase 0): **101 passed, 5 skipped** (integration tests skip without a broker), coverage **73 %**. Counts may never go down.

## Non-negotiables
- [ ] N1 Every surface is energy: grid leads README, first screenshot, quickstart, demo
- [ ] N2 Water profile deleted; crude oil pipeline pump station profile replaces it (zero water vocabulary outside git history)
- [x] N3 Decisions recorded in docs/DECISIONS.md as they are made
- [ ] N4 Everything committed

## Checklist A — judge-facing
- [ ] A1 Energy-first README (first 60 lines grid-only)
- [ ] A2 docs/DEMO.md + `make demo` asserting every verdict + 30-second fallback
- [ ] A3 `make metrics` generates README numbers between markers; CI fails on drift
- [ ] A4 Atomic commit history; tag v1.0.0-icsc; README shows the tag
- [ ] A5 Docker path proven from a fresh clone with healthchecks
- [ ] A6 Zero network at runtime: self-hosted woff2 fonts, no CDN, CSP, verified with outbound blocked
- [ ] A7 Auth on every mutating endpoint (token generated at first run) + docs/THREAT-MODEL.md
- [ ] A8 MIT LICENSE with correct year and holder
- [ ] A9 One flagship: grid; pipeline behind `--profile pipeline`
- [ ] A10 docs/RESULTS.md generated by the evaluation harness; headline numbers in README and console About panel

## Checklist B — build
- [ ] B1 Modbus TCP primary: native server for the RTUs + passive tap parsing FC 1/2/3/4/5/6/15/16; MQTT as an adapter
- [ ] B2 Per-source HMAC-signed command envelopes; SRC-001 verifies signatures; residual risk in THREAT-MODEL.md
- [ ] B3 Replay defeat: per-source monotonic counter + nonce cache + freshness window; test that a replayed valid command is rejected
- [ ] B4 Learned baseline: cadence EWMA, setpoint quantiles, command mix per source; console shows learned vs configured; trained on the legitimate corpus in RESULTS.md
- [ ] B5 Real grid physics: forward-backward sweep power flow, two feeders + tie, per-section permits, concurrent faults, hand-computed validation test
- [ ] B6 SQLite persistence for guard state + restart test
- [ ] B7 Narration off the operator timeline: director panel, OFF in judge mode
- [ ] B8 Light/dark × desktop/phone parity, screenshots committed
- [ ] B9 Pinned requirements with hashes, lock file, base image by digest; fresh venv + fresh docker build

## Verification matrix (every iteration)
| ID | Check | Status |
|---|---|---|
| V1 | Full test suite green, count ≥ baseline | |
| V2 | `make demo` passes end to end | |
| V3 | Fresh clone, `docker compose up`, console reachable, scenario runs, teardown clean | |
| V4 | Fresh venv from pinned requirements installs and runs | |
| V5 | Console loads with outbound network blocked | |
| V6 | Unauthenticated mutating request rejected; authenticated succeeds | |
| V7 | Replay rejected; spoofed source rejected | |
| V8 | Restart preserves state | |
| V9 | Water vocabulary grep: zero hits outside git history | |
| V10 | README first 60 lines grid-only | |
| V11 | `make metrics` output matches committed README and RESULTS.md | |
| V12 | Light + dark × desktop + phone render cleanly | |
| V13 | Hostile fresh-clone walkthrough surfaced no new issues | |
